# General
OPENAI_API_KEY=your-key-here

# Backend
# CRITICAL: Use a strong, unique secret key for production
SECRET_KEY=prod-environment-very-secure-key-change-me
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
BACKEND_CORS_ORIGINS=["https://app.traceiq.example.com"]

# Frontend
# Points to the PROD environment API
VITE_API_BASE_URL=https://api.traceiq.example.com/api

# Execution Engine
ARTIFACTS_DIR=/tmp/artifacts
DEFAULT_TIMEOUT=60000

# Infrastructure
MINIO_ROOT_USER=prod_minio_user
MINIO_ROOT_PASSWORD=prod_minio_very_secure_password
POSTGRES_USER=prod_db_user
POSTGRES_PASSWORD=prod_db_very_secure_password
