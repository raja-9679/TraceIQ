fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlmodel==0.0.14
celery==5.3.6
# Redis-backed beat scheduler with a lock, so more than one celery_beat can run
# (workstream H1). Opt-in via CELERY_BEAT_SCHEDULER — a single beat is still a
# single point of failure that silently stalls the whole execution pipeline.
celery-redbeat==2.2.0
redis==5.0.1
playwright==1.41.1
boto3==1.34.23
openai==1.10.0
asyncpg==0.29.0
psycopg2-binary==2.9.9
python-multipart==0.0.20
pydantic-settings==2.1.0
httpx==0.26.0
requests==2.32.4
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.4.0
croniter==2.0.2
slowapi==0.1.9
anthropic==0.39.0
PyYAML==6.0.1
# LDAP / on-prem Active Directory login (pure Python — no libldap needed)
ldap3==2.9.1
# SAML 2.0 (workstream F3). xmlsec ships manylinux wheels that bundle
# libxmlsec1/libxml2, so no apt packages are needed; the three are pinned
# together because an xmlsec wheel built against a different libxml2 than
# lxml's segfaults at import.
python3-saml==1.16.0
xmlsec==1.3.17
lxml==6.1.3
# Observability (workstream H4): OTLP traces to any collector, optional Sentry.
# All no-ops until OTEL_EXPORTER_OTLP_ENDPOINT / SENTRY_DSN are set.
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
opentelemetry-instrumentation-sqlalchemy==0.48b0
opentelemetry-instrumentation-httpx==0.48b0
opentelemetry-instrumentation-redis==0.48b0
opentelemetry-instrumentation-celery==0.48b0
sentry-sdk[fastapi,celery,sqlalchemy]==2.19.0

alembic==1.13.1
